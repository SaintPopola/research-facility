<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- HISE/JUCE plugins load a compiled DSP dylib and run inside third-party
       DAW processes, so library validation must be disabled and unsigned
       executable memory allowed. No app-sandbox key: audio plugins are not
       sandboxed (that is a Mac App Store concern, which does not apply here). -->
  <key>com.apple.security.cs.disable-library-validation</key><true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
  <key>com.apple.security.files.user-selected.read-write</key><true/>
</dict>
</plist>
